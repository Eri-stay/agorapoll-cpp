import '../../../data/models/poll_model.dart';

// Наш список захардкоджених опитувань
final List<Poll> mockPolls = [
  // Poll(question: 'What day works best for the meeting?'),
  // Poll(question: 'Which restaurant should we choose?'),
  // Poll(question: 'Best time for the team lunch?', isClosed: true),
];
